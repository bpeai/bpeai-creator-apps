# Equipment evaluator stub (EXAMPLE ONLY)

LLM bootstrap seed for `equipment_evaluator` packs. This is **not** a production
knowledge pack.

- Authoring seed: this folder (`equipment_evaluator_stub`)
- SDK unit-test fixture: `_examples/mixing_stub/` (do not point bootstrap at it)

`scenario_id` names a **host equipment system** (process vessel, process skid,
CIP return pump, chromatography skid). It is not an operation such as transfer
or venting. `industries` use `project_definition_sectors` labels.
