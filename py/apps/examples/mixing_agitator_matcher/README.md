# LEGACY — do not copy

This example uses **in-code DIR tables** and an older evaluation prompt. It is kept
for historical / first-party reference only.

**Start new apps from:** `py/apps/_templates/equipment_evaluator/`  
**SME content:** `py/knowledge/<system>/`

Portal module path for this legacy app (if still deployed):
`apps.examples.mixing_agitator_matcher.agent` is **not** the convention for new
creator apps — new apps use `apps.<your_id>.agent`.
